# Release Notes

Release notes for the Askalot platform, one Markdown file per release, named `YYYY-MM-DD.md`. Platform releases are date-versioned (the release date **is** the version); newest release sorts last alphabetically. Each note is also published as a GitHub Release on this repository, so **Watch → Custom → Releases** notifies you of new ones.
